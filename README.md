# AgentTrace

AgentTrace is a visual debugger for agentic workflows. It acts as a "flight data recorder" for AI agents, synchronizing execution state (like UI screenshots or DOM state) with the exact LLM prompt and response.

## Getting Started

*(Instructions will be added as the project is developed)*
